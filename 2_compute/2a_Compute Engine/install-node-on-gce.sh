# -- to install Node JS on a GCE default instance -- #

1. Create a default GCE instance and SSH to that instance
2. Install Node JS via the SSH terminal window using `sudo apt install nodejs`
3. Verify installation using `node -v`